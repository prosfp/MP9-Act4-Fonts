name: Validate HTML and CSS

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Validate HTML
        uses: anishathalye/proof-html@v2
        with:
          directory: ./index.html
